# Andrew Inputs Needed: {{project name}}

## Before the build starts (0 minutes — decisions only, defaults are fine)

| # | Decision | Default (used if you say nothing) |
|---|---|---|
| D1 | {{decision}} | {{default}} |
| D2 | … | … |

## A1 — {{input name}} (Wave {{n}}, gates {{T-ids}}) — ~{{minutes}} min

**What:** {{numbered, concrete steps only Andrew can do — logins, keys, console clicks, purchases.}}

**How to signal:** {{mechanism: drop a file in the plan folder, reply in session, or "just leave it running — Tn's probe fails fast with a clear error if not ready".}}

**Associated tasks:** {{T-ids}}.

<!-- One section per A-item, ordered by when the build needs it. Every 🧑 marker in doc 02
     must trace to exactly one A-item. Give a default/fallback wherever one exists. -->

## Timeline at a glance

{{One line per checkpoint: "A1+A2 before Wave 3 · A3 before Wave 5 · A4 closes T12." State what
proceeds without them.}}

## Ongoing (after the build)

{{Recurring obligations the design creates: monthly costs, key rotation, quota watching.}}
